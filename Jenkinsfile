pipeline {
    agent any

    stages {
        stage('Git clon') {
            steps {
                git branch: 'main', url: 'https://github.com/murulii/youtube_app.git'
            }
        }
    }
}
